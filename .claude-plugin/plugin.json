{
  "name": "usage-guard",
  "version": "0.3.0",
  "description": "Мониторит потребление лимитов подписки Claude Code и на пороге автоматически просит агента поставить cron-триггер, чтобы продолжить работу после сброса лимита.",
  "author": {
    "name": "adyachenko"
  },
  "homepage": "https://github.com/adyachenko/claude-code-usage-guard",
  "repository": "https://github.com/adyachenko/claude-code-usage-guard",
  "license": "MIT",
  "keywords": [
    "usage",
    "rate-limit",
    "ccusage",
    "scheduling",
    "cron",
    "automation"
  ],
  "userConfig": {
    "block_pct": {
      "title": "Порог блокировки (%)",
      "type": "number",
      "description": "При достижении этого процента 5-часового окна плагин блокирует tool call и просит агента поставить CronCreate-триггер. Рекомендовано 95–98.",
      "default": 98,
      "sensitive": false
    },
    "warn_pct": {
      "title": "Порог предупреждения (%)",
      "type": "number",
      "description": "При этом проценте в stderr печатается warning (агенту видно), но вызов пропускается.",
      "default": 90,
      "sensitive": false
    },
    "mode": {
      "title": "Режим расчёта лимита",
      "type": "string",
      "description": "auto — ccusage max-of-history; fixed — token_limit; cost — cost_limit в USD (рекомендовано, автоматически взвешивает Opus/Sonnet/Haiku). Калибруется через /usage-guard:calibrate.",
      "default": "auto",
      "sensitive": false
    },
    "token_limit": {
      "title": "Лимит токенов на 5-часовое окно",
      "type": "number",
      "description": "Только при mode=fixed. Жёсткий лимит в токенах.",
      "default": 220000000,
      "sensitive": false
    },
    "cost_limit": {
      "title": "Лимит в $USD на 5-часовое окно",
      "type": "number",
      "description": "Только при mode=cost. Устанавливается командой /usage-guard:calibrate.",
      "default": 0,
      "sensitive": false
    },
    "throttle_seconds": {
      "title": "Минимальный интервал между проверками (сек)",
      "type": "number",
      "description": "Реже запускать ccusage, чтобы снизить накладные расходы хука.",
      "default": 30,
      "sensitive": false
    },
    "block_on": {
      "title": "Метрика для блокировки",
      "type": "string",
      "description": "current — блокировать по текущему потреблению; projected — по прогнозу полного 5h-окна (preemptive, рекомендовано).",
      "default": "projected",
      "sensitive": false
    },
    "resume_prompt": {
      "title": "Prompt при возобновлении",
      "type": "string",
      "description": "Текст, который придёт агенту, когда CronCreate-триггер сработает после сброса лимита.",
      "default": "Лимит сброшен. Продолжай работу с того места, где остановился до паузы по usage-guard.",
      "sensitive": false
    }
  }
}
