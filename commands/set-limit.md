---
description: Задать реальный token-лимит 5-часового окна (переключает в fixed). Пример: /usage-guard:set-limit 44m
argument-hint: "<tokens>"
---

Запусти `${CLAUDE_PLUGIN_ROOT}/scripts/set-limit.sh $ARGUMENTS` через Bash и покажи результат пользователю как есть.
