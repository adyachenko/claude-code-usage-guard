---
description: Установить процент срабатывания блокировки usage-guard (по умолчанию 98). Пример: /usage-guard:set-threshold 95
argument-hint: "<pct>"
---

Запусти `${CLAUDE_PLUGIN_ROOT}/scripts/set-threshold.sh $ARGUMENTS` через Bash и покажи результат пользователю как есть.
